query registrationForm($eventID:Int!) {
  forms(id: [$eventID]) {
    title
    description
    questions {
      title
      type
      required
      answers {
        ID
        title
        points
      }
    }
  }
}
